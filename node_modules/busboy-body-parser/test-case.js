const app = require('express')();

app.use(require('./')({ limit: '5gb' }));

app.use((req, res) => {
  res.json(req.files && Object.keys(req.files).map(key => [ req.files[key].name, req.files[key].size ]));
});

app.listen(3001);
